# Distributed Video Processing Infrastructure

Kubernetes deployment notes live in `k8s/`, including the Azure AKS demo overlay at `k8s/overlays/azure-aks/README.md`, the Oracle Always Free k3s plan at `k8s/oracle-k3s-deployment.md`, the Civo plan at `k8s/civo-deployment.md`, the local kind smoke test at `k8s/local-kind.md`, and the environment reference at `k8s/env-reference.md`.
