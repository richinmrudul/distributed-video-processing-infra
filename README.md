# Distributed Video Processing Infrastructure

## Configuration

Use `.env.example` as the production-style configuration reference. Docker Compose remains the local development path; production deployments should set `APP_ENV=production` and provide real secrets for Postgres, Redis, object storage, and `ADMIN_API_KEY`.
